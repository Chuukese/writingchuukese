<%= form_for(@chuuk) do |f| %>
  <% if @chuuk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chuuk.errors.count, "error") %> prohibited this chuuk from being saved:</h2>

      <ul>
      <% @chuuk.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :search %><br />
    <%= f.text_field :search %>
  </div>
  <div class="field">
    <%= f.label :spelling %><br />
    <%= f.text_area :spelling %>
  </div>
  <div class="field">
    <%= f.label :region %><br />
    <%= f.text_area :region %>
  </div>
  <div class="field">
    <%= f.label :picture %><br />
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
